# Omni-Script ShellCheck Configuration
# ═══════════════════════════════════════════════════════════════════════════════

# Default shell is bash
shell=bash

# Disable SC2034 - unused variables (constants.sh exports for use by other scripts)
disable=SC2034

# Enable all optional checks
enable=all

# Allow sourcing from the same directory
source-path=SCRIPTDIR

# Allow external sources (for sourcing our lib files)
external-sources=true

# Severity level (style = most pedantic)
severity=style
